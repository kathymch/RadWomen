public with sharing class CommentingOnCodeExercise {

    /**
     * Your Assignment is to add comments describing what is being done in the methods below.
     * Call out the concepts you learned in your readings and in class.
     */

    public static void cartValues() {

        //Declare an Integer variable (minimumCartValue) and assign a value in the same line, which is: 50.
        Integer minimumCartValue = 50;

        //Declare 3 Integer variables (itemA, itemB, itemC) and assign a value in the same line for each one.
        Integer itemA = 10;
        Integer itemB = 20;
        Integer itemC = 45;

        //Sum of the values: itemA and itemB (10+20)
        Integer cartValue = itemA + itemB;

        //if cartValue is greater than minimumCartValue result = true
        Boolean cartMinimumMet = cartValue >= minimumCartValue;
        
        //debug log for the minimum of cartMinimumMet variable value
        System.debug('Have we reached the minimum: ' + cartMinimumMet);

        //The value for cartValue is the result of the sum of the current cartValue and itemC
        cartValue = cartValue + itemC;

        //The value for cartMinimumMet is when cartValue is greater than minimumCartValue  
        cartMinimumMet = cartValue >= minimumCartValue;

        //debug log for the result of cartMinimumMet 
        System.debug('How about now? : ' + cartMinimumMet);

    }


}
